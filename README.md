A simple **console-based calculator** written in Java.  
This program performs basic arithmetic operations: addition, subtraction, multiplication, division, and modulus.

Features

- Add two numbers (`+`)
- Subtract two numbers (`-`)
- Multiply two numbers (`*`)
- Divide two numbers (`/`)
- Modulus of two numbers (`%`)
- Handles invalid operators with a warning message
